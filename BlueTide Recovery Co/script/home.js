// Grab the button by its ID
const orderButton = document.getElementById('services');

// Add a click event
orderButton.addEventListener('click', function() {
    // Change the location to the page you want
    window.location.href = 'services.html';
});
